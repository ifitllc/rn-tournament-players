// Google Drive authentication UI was removed. Supabase handles storage now.
// Keeping an empty component to avoid import errors if any stale references remain.
export default function GoogleDriveAuth() {
  return null;
}
